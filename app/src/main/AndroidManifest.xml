<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.innovach.echeckin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Attendance List"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MC2">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.innovach.echeckin.MainActivity" />
        </activity>
        <activity android:name=".MC3" />
        <activity android:name=".MPFT4" />
        <activity android:name=".MPPT" />
        <activity android:name=".qr.ReaderActivity" />
        <activity android:name=".qr.GeneratorActivity"></activity>
    </application>

</manifest>